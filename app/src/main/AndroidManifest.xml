<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tushar.trial2kriger">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.Register" />
        <activity android:name=".Activities.Splash" />
        <activity android:name=".Activities.Tut1" />
        <activity android:name=".Activities.Tut2" />
        <activity android:name=".Activities.Tut3" />
        <activity android:name=".Activities.PhoneAuthActivity" />
        <activity android:name=".Activities.SplashActivity" />
        <activity android:name=".Activities.ImagesListActivity" />
        <activity android:name=".Activities.AddImagesActivity"></activity>
    </application>

</manifest>